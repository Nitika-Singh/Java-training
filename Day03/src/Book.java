public class Book {
    String title;
    double price;

    //Constructor
    //Special function
    //has the same name as the class
    //does not have return value
    //called ONLY ONCE and that too during the creation of an object
    Book(String theTitle, double thePrice) {
        title = theTitle;
        price = thePrice;
    }
}
